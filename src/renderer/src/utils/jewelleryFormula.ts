// Re-exported from the shared single source of truth so the renderer's live
// preview computes jewellery math identically to what the backend saves.
// Do not add logic here — edit src/shared/jewelleryFormula.ts instead.
export * from '../../../shared/jewelleryFormula'
export type { LabourRateType } from '../../../shared/jewelleryFormula'
